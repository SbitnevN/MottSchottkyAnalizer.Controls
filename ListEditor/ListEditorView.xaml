<Window x:Class="MottSchottkyAnalizer.Controls.ListEditor.ListEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MottSchottkyAnalizer.Controls.ListEditor"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="450" Width="800">
    <Grid>
        <DataGrid ItemsSource="{Binding Items, Mode=OneWay}"
                  CanUserAddRows="True"
                  CanUserDeleteRows="True"
                  Margin="10,10,10,75"/>

        <Button Content="Ok" Command="{Binding SubmitCommand, Mode=OneWay}" HorizontalAlignment="Left" Margin="10,386,0,0" VerticalAlignment="Top" Width="153"/>
        <Button Content="Cancel" Command="{Binding CancelCommand, Mode=OneWay}" HorizontalAlignment="Left" Margin="189,386,0,0" VerticalAlignment="Top" Width="153"/>
    </Grid>
</Window>
