<Border
    x:Class="MottSchottkyAnalizer.Controls.Controls.Drawer.DrawerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:MottSchottkyAnalizer.Controls.Controls.Drawer"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{StaticResource PanelBrush}"
    mc:Ignorable="d">

    <i:Interaction.Behaviors>
        <local:DrawerAnimationBehavior
            IsOpen="{Binding IsDrawerOpen}"
            Placement="{Binding HorizontalAlignment, RelativeSource={RelativeSource AncestorType=Border}}"
            Duration="0:0:0.25" />
    </i:Interaction.Behaviors>
</Border>
